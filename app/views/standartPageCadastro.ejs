<div class="cabecalhoCadastro">
    <p>Cadastro de ...</p>
    <div class="filtrar">
        <label for="filtrar">Filtrar por:</label>
        <input type="text" name="filtrar" id="filtrar">
    </div>
    <div class="pesquisar">
            <button><img src="img\icons\search.svg" alt="" title="Pesquisar"></button>
            <button id="novoRegistro"><img src="img\icons\add.svg" alt="" title="Novo Registro"></button>
    </div>
</div>

<div class="listaDados">
    <table class="tbLista">
        <tr class="cabecalhoLista">
            <th class="thId">Id</th>
            <th class="thDescricao">Descricao</th>
            <th class="thSituacao">Situação</th>
            <th class="thAcoes">Ações</th>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
        <tr v-for="i in lista" :key="i.id">
            <td class="tdId">{{ .id }}</td>
            <td class="tdDescricao">{{ i.nome }}</td>
            <td class="tdSituacao">{{ i.fone }}</td>
            <td class="tdAcoes">
                <img src="/img/icons/visualizar.svg" @click="visualizar">
                <img src="/img/icons/editar.svg" @click="editar">
                <img src="/img/icons/deletar.svg"  @click="excluir">
            </td>
        </tr>
    </table>
</div>
